#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    
    // Read three space-separated integers from stdin
    if (cin >> a >> b >> c) {
        // Calculate and print the sum to stdout
        cout << a + b + c << endl;
    }
    
    return 0;
}
